import 'package:flutter/material.dart';

class BusinessPendingDeletion extends StatelessWidget {
  const BusinessPendingDeletion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Business pending deletion'));
  }
}